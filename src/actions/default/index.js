import {getDefaultOptions, defaultOptionsRequested} from "./option";
import {setObject, getObject, updateObject, defaultEditSubmit,defaultDetailLoaded,deleteObject} from "./detail";
import {getList, deleteObjectListAction} from "./list";

export {
    getDefaultOptions,
    defaultOptionsRequested,
    setObject,
    defaultEditSubmit,
    deleteObjectListAction,
    getList,
    getObject,
    updateObject,
    defaultDetailLoaded,
    deleteObject
}
