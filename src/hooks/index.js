import UseScript from "./use-script";
import useLink from "./use-link";
import {setCookie, getCookie, removeCookie} from "./cookie";
import {setLocalStorage, getLocalStorage, removeLocalStorage, clearLocalStorage} from "./local-storage";

export {
    UseScript,
    useLink,
    setCookie,
    getCookie,
    removeCookie,
    setLocalStorage,
    getLocalStorage,
    removeLocalStorage,
    clearLocalStorage
};
